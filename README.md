# Reboot D-Link DIR-300 router remotely

## Usage

IP address is **192.168.0.1** and username is **admin** by default
```bash
$ ./reboot.sh
```

With the specified IP address and username
```bash
$ ./reboot.sh 192.168.1.1 admin
```
